// ROUTE STUB — retreats page ("/retreats").
// The route exists so the nav link resolves. Content for this page has not been
// scoped or written yet and is not to be invented here.

import RetreatsIntro from "@/components/sections/RetreatsIntro";

export default function RetreatsPage() {
  return <RetreatsIntro />;
}
